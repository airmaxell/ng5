import { Route } from '@angular/router';
import { CatchersComponent } from './catchers.component';

export const CatchersRoutes: Route[] = [
    {
      path: 'catchers',
      component: CatchersComponent
    }
];
